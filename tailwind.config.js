/** @type {import('tailwindcss').Config} */
module.exports = {
	content: ["./src/**/*.{js,jsx,ts,tsx}"], // Only this block is necessary
	theme: {
		extend: {}, // Extend this to add custom themes if needed
	},
	plugins: [], // Add plugins here if required
};
